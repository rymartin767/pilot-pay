<?php

test('user can be an admin', function () {
    expect(true)->toBeTrue();
})->todo();

test('user with verified email can access their filament panel', function () {
    expect(true)->toBeTrue();
})->todo();

test('user has reports', function () {
    expect(true)->toBeTrue();
})->todo();
